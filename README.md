Pasos para ejecutar:

npm install
npm run dev

Abrir enlace

ENDPOINTS:

Verificar un usuario
GET /api/verificar?codigo={Codigo de usuario}

Listar todos los usuarios
GET /api/usuarios

Buscar usuario por código
GET /api/usuarios/USR-001

Info general de la API
GET /